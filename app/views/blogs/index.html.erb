<% provide(:title, "Small Blogs") %>

<div class="row align-center">
  <div class="small-12 medium-10 large-8">
    <% if @blogs.any? %>
      <%= render @blogs %>
    <% else %>
      <p class="mute text-center">No blogs have been started</p>
    <% end %>
  </div>
</div>
